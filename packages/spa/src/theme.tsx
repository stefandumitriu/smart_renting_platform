import {createTheme} from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#d7cec7'
        },
        secondary: {
            main: '#76323e'
        },
        text: {
            primary: '#565656'
        }
    }
});

export default theme;