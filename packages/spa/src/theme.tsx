import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#fbad14",
    },
    secondary: {
      main: "#1890FF",
    },
    text: {
      primary: "#565656",
    },
    background: {
      default: "#FAFAFB",
      paper: "#fff",
    },
  },
});

export default theme;
